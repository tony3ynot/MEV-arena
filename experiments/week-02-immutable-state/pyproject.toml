[project]
name = "week-02-immutable-state"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["simulation-engine"]

[tool.uv]
package = false

[tool.uv.sources]
simulation-engine = { workspace = true }
