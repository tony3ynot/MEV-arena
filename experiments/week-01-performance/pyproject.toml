[project]
name = "week-01-performance"
version = "0.0.0"
description = "Learning experiment: pool-gated server under open-model load; see README.md."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
]

[tool.uv]
package = false
